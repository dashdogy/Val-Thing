VAL_BRIDGE_HOST=0.0.0.0
VAL_BRIDGE_PORT=8787
VAL_BRIDGE_MAX_CONCURRENCY=16
VAL_BRIDGE_REQUEST_TIMEOUT_MS=300000
# Zero disables the Responses timeout; otherwise reasoning chunks refresh it.
VAL_BRIDGE_RESPONSE_TIMEOUT_MS=0
# Comma-separated exact browser origins. Empty disables browser CORS.
VAL_BRIDGE_CORS_ORIGINS=
