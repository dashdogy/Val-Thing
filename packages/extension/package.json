{
  "name": "@val-bridge/extension",
  "version": "0.1.5",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:release": "node scripts/build.mjs --release",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test test/*.test.ts"
  },
  "dependencies": {
    "@val-bridge/protocol": "*",
    "socket.io-client": "^4.8.1"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.326",
    "esbuild": "^0.28.1",
    "sharp": "^0.35.3",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=24"
  }
}
